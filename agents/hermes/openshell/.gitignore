# Site-specific files (copy from .example templates)
hermes.env
policy.yaml
glab-config.yml
jirahhh-config.yaml
extra-ca-certs.pem
kubeconfig

# Supervisor comes from an OpenShell supervisor *image* via COPY --from
# (see Containerfile.kubevirt). Do not stage a raw binary here.
openshell-sandbox
