apiVersion: v1
kind: Service
metadata:
  name: hermes-dashboard
  labels:
    app: hermes-agent
    component: dashboard
spec:
  type: ClusterIP
  ports:
  - port: 9119
    targetPort: 9119
    protocol: TCP
    name: http
  selector:
    app: hermes-agent
    component: dashboard
